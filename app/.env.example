# Database Configuration - PostgreSQL
# Use strong passwords and enable SSL in production
DATABASE_URL="postgresql://username:password@host.neon.com/database_name?sslmode=require"

# NextAuth.js Configuration
# Generate secure random string: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="https://your-app-name.vercel.app"

# Email Configuration (Gmail SMTP)
# Use App Passwords, not regular passwords
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-app-password"
ADMIN_EMAIL="admin@your-domain.com"

# Stripe Configuration (for payments)
# Keep secret keys secure and never commit to version control
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_publishable_key"
STRIPE_SECRET_KEY="sk_test_your_secret_key"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret"

# CRM Integration Configuration
# Generate secure API key: openssl rand -hex 32
CRM_API_KEY="your-secure-api-key-here"

# Security Notes:
# 1. NEVER commit actual credentials to version control
# 2. Use strong, unique passwords for all services
# 3. Enable 2FA on all accounts
# 4. Rotate credentials regularly (every 90 days)
# 5. Use environment-specific values for each deployment