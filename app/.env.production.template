# ⚠️ PRODUCTION ENVIRONMENT TEMPLATE ⚠️
# DO NOT COMMIT ACTUAL CREDENTIALS TO VERSION CONTROL
# Copy this file to .env.production and replace all placeholder values

# Production Database Configuration
DATABASE_URL="postgresql://username:password@your-host.neon.tech/database?sslmode=require"

# NextAuth.js Configuration - GENERATE NEW SECURE RANDOM STRINGS
NEXTAUTH_SECRET="REPLACE_WITH_SECURE_RANDOM_STRING"
NEXTAUTH_URL="https://your-actual-domain.com"

# Email Configuration (Gmail SMTP)
EMAIL_USER="your-actual-email@gmail.com"
EMAIL_PASS="your-actual-app-password"
ADMIN_EMAIL="admin@your-domain.com"

# CRM Integration Configuration - GENERATE NEW API KEY
CRM_API_KEY="REPLACE_WITH_SECURE_API_KEY"
NEXT_PUBLIC_APP_URL="https://your-actual-domain.com"

# Security Notes:
# 1. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 2. Generate CRM_API_KEY: openssl rand -hex 32
# 3. Use strong unique passwords for database
# 4. Enable SSL/TLS for all connections
# 5. Never commit this file with real values